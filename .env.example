# ---- Paths (relative to repo root are fine) ----
DATA_DIR=data/parquet
MODELS_DIR=models/faiss
PROMPTS_DIR=prompts

# ---- Data metadata ----
DATA_YEAR=2025
DATA_CURRENCY=USD

# ---- LLM backend (choose: gemini | ollama | disabled) ----
LLM_BACKEND=gemini

# Gemini (default for Streamlit Cloud)
GEMINI_MODEL=gemini-1.5-flash-latest
GOOGLE_API_KEY=<my_key>

# Ollama (optional, local use)
# OLLAMA_MODEL=llama3.1:8b

# Common generation params
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=400

# ---- App behavior ----
COMPARE_LIMIT=5
DEFAULT_SAME_INDUSTRY=true
DEFAULT_SAME_REGION=false

# ---- Optional UI bits (only if you use them) ----
# BRAND_BANNER=assets/banner.png
# FOOTER_NOTE=All figures from Forbes Global 2000 (2025); USD.